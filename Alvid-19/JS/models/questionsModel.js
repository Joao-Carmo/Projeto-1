export default class questionsModel {
    constructor(id, title, photo, answers, correctAnswer) {
        this.id = id
        this.title = title
        this.photo = photo
        this.answers = answers
        this.correctAnswer = correctAnswer
    }
}